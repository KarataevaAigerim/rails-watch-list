<h1><%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f|%>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Add a bookmark" %>
<% end %>

<%= link_to "Back", list_path(@list), class: "btn btn-primary" %>
